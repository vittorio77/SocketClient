<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"

    android:id="@+id/popup_layout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@color/DarkGray"
    android:focusedByDefault="true">

    <EditText
        android:id="@+id/popupTextPassword"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:cursorVisible="true"
        android:inputType="textPassword"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />

    <Button
        android:id="@+id/btn_OkPassword"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@id/popupTextPassword"
        android:layout_below="@id/popupTextPassword"
        android:layout_marginRight="30dp"
        android:elevation="12dp"
        android:text="OK"
        android:textStyle="bold" />

    <Button
        android:id="@+id/btn_CancelPassword"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/popupTextPassword"
        android:layout_below="@id/popupTextPassword"
        android:layout_marginLeft="30dp"
        android:elevation="12dp"
        android:text="CANCEL"
        android:textStyle="bold" />


</RelativeLayout>